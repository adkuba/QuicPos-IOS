mutation View($userID: Int!, $postID: String!, $time: Float!, $device: String!, $password: String!) {
  view(
    input: {
        postID: $postID,
        userId: $userID,
        time: $time,
        deviceDetails: $device
    }
    password: $password
  )
}
