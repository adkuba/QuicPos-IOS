mutation CreatePost($text: String!, $userId: Int!, $image: String!) {
  createPost(
    input: {
      text: $text
      userId: $userId
      image: $image
    }
  ) {
    ID
    text
    userId
    shares
    views
    creationTime
    initialReview
    image
  }
}
