
query GetPost($userId: String!, $normalMode: Boolean!) {
  post(userId: $userId, normalMode: $normalMode) {
    ID
    text
    userId
    shares
    views
    creationTime
    initialReview
    image
  }
}
