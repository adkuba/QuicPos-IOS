
query GetPost($userID: Int!, $normalMode: Boolean!) {
  post(userId: $userID, normalMode: $normalMode) {
    ID
    text
    userId
    shares
    views
    creationTime
    initialReview
    image
  }
}
